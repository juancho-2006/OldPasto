# ~/.config/hypr/hypridle.conf

listener {
    timeout = 240
    on-timeout = hyprlock
}

listener {
    timeout = 420
    on-timeout = hyprctl dispatch dpms off
    on-resume = hyprctl dispatch dpms on
}

listener {
    timeout = 600
    on-timeout = ~/.config/hypr/hibernar_pregunta.sh
}
